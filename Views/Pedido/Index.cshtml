@model IEnumerable<avd4.Models.Pedido>

@{
    ViewData["Title"] = "Index";
}

<h1>Lista de Pedidos</h1>

<p>
    <a asp-action="Create" class="btn btn-primary btn-sm">Novo Pedido</a>
</p>
<div class="row">
    <div class="col-md-12">
        <div class="card">
            <div class="card-body">
                <table class="table table-striped table-sm">
                    <thead>
                        <tr>
                            <th>
                                #
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Cliente)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Produto)
                            </th>

                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@Html.DisplayFor(modelItem => item.Id)</td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Cliente.Nome)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Produto.Nome)
                                </td>

                                <td>
                                    <a asp-action="Edit" asp-route-id="@item.Id" class="badge badge-primary">Editar</a> |
                                    <a asp-action="Details" asp-route-id="@item.Id" class="badge badge-info">Detalhes</a> |
                                    <a asp-action="Delete" asp-route-id="@item.Id" class="badge badge-danger">Deletar</a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
